name: First Workflow
on: workflow_dispatch #this mean manually started
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello Pasaulis"
      - name: Print good-bye
        run: echo "Done Stepsas"
