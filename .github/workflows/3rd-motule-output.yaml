name: Test and deploy project(React 3rd Module)
on: workflow_dispatch
jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Output GitHub Context
        run: echo "GitHub Context: ${{ toJson(github) }}" # Outputs the GitHub context to the console  toJSON GitHub funkcija